server:
  port: ${PORT:8686}
  servlet:
    context-path: /pleiepenger-barn-uttak
no.nav.security.jwt:
  issuer:
    azure:
      discoveryurl: http://vtp:8060/rest/azuread/.well-known/openid-configuration
      accepted_audience: vtp
