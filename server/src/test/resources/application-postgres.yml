server:       
  port: ${PORT:8686}
  servlet:
    context-path: /pleiepenger-barn-uttak
no.nav.security.jwt:
  issuer:
    azure:
      discoveryurl: http://localhost:${mock-oauth2-server.port}/rest/azuread/.well-known/openid-configuration
      accepted_audience: vtp